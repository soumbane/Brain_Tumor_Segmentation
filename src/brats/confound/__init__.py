"""Confound analysis. A first-class module, not a notebook.

If this analysis lives in a notebook it will not be maintained, and it is not
optional: in pooled BraTS 2023 the tumor-type label *is* the cohort folder, so the
classification result is uninterpretable without it.
"""
